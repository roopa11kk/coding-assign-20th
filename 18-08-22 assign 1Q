#include<stdio.h>
void main()
{
	int n;
	int num[5];
	int flag=0;
	printf("Enter the size of the array:");
	scanf("%d",&n);
	printf("Enter array elements\n");
	scanf("%d",&num);
	int i=num;
	while(n>1)
	{
		printf("Enter array elements\n");
		scanf("%d",&num);
		if(i==num)
		{
			flag=1;
		}
		n--;
	}
	if(flag==0)
	printf("DUPLICATE\n");
	else
	printf("UNIQUE\n");
}
